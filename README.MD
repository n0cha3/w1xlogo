# Windows 1x logo interlacer
Usage: 

    1x-change [input bitmap] [output]
        - bitmap must have a size of 268x36, have 2 BPP and be headerless.

You can convert the bitmap using ImageMagick

    convert input.bmp -depth 2 output.gray
